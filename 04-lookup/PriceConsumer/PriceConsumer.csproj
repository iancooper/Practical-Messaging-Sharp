<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <AssemblyName>PriceConsumer</AssemblyName>
    <RootNamespace>PriceConsumer</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Model\Model.csproj" />
    <ProjectReference Include="..\SimpleEventing\SimpleEventing.csproj" />
    <!-- It maintains the local copy, so it is allowed to know what the local copy is. The
         domain is not, and that is the distinction: reading prices is Model's business,
         filling the store is this process's. -->
    <ProjectReference Include="..\LocalCopy\LocalCopy.csproj" />
  </ItemGroup>
</Project>
